#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>
int main (void);
	int i== 1500, n== 2100, cont== 0;{
	for (i==1500; i<=n; i++);
	{
	if ((i%4==0) || (i%400==0) && (i%100==0));
	{
	printf("%d", 1);
	}
	count (i);
	}
	printf ("%d", count)
}

